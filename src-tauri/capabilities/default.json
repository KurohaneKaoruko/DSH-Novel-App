{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "DSH-Novel 主窗口（不使用 Tauri IPC——文件面板走本地 side-channel HTTP）",
  "windows": ["main"],
  "permissions": ["core:default"]
}
